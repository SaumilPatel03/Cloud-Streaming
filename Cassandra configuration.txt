# Create Keyscape
CREATE KEYSPACE IF NOT EXISTS media_keyspace 
WITH replication = {'class': 'SimpleStrategy', 'replication_factor': 1};

# Table
CREATE TABLE IF NOT EXISTS media_keyspace.media (
    media_id INT PRIMARY KEY,
    title TEXT
);

